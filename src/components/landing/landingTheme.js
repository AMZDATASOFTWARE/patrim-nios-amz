/** Shared style tokens for the futuristic landing (same values used across landing components). */
export const display = { fontFamily: 'var(--font-display)' };
export const dim = 'hsl(200 30% 96% / 0.65)';
export const dimmer = 'hsl(200 30% 96% / 0.4)';
export const bright = 'hsl(200 30% 96% / 0.75)';
